{
  "name": "ts-knowledge-base",
  "version": "1.0.0",
  "description": "Personal TypeScript/JavaScript knowledge base: Total TypeScript book exercises, worked-example modules, algorithms, and performance notes",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:types": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/ui": "^4.1.5",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
